# Facets Honeypot Module

A Drupal module that adds a hidden honeypot checkbox to all facets with the label 'This will ban you'. This checkbox is designed to catch bots that automatically click on all form elements.

