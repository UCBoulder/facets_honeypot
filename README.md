# Facets Honeypot Module

A Drupal module that adds a hidden honeypot checkbox to all facets with the label 'Not an option'.

Checking this box will add a message in watchdog logs indicating a potential bot interaction.

This checkbox is designed to catch bots that automatically click on all form elements.

A user can use these log entries to ban users using autoban.

